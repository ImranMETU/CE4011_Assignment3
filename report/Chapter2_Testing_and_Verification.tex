\chapter{Testing and Verification}

\section{Testing and Verification Framework}

To ensure the correctness and robustness of the developed structural analysis software, a structured testing and verification strategy was adopted. The objective is to validate both the numerical implementation of the Direct Stiffness Method and the correct interaction between object-oriented components.

The testing framework is divided into three categories:

\begin{itemize}
    \item \textbf{Unit Tests}: Verify the correctness of individual components, particularly element-level formulations such as stiffness matrices, transformation matrices, and equivalent nodal load vectors.

    \item \textbf{Interface Tests}: Validate the interaction between components, including global stiffness matrix assembly, DOF mapping, and load vector construction.

    \item \textbf{Regression Tests}: Ensure that the complete system produces accurate results for known structural problems, including frame, truss, and mixed frame-truss configurations.
\end{itemize}

All tests are automated and executed using a test suite. Numerical checks use the same assertion policy as the codebase: relative tolerance $10^{-6}$ and absolute tolerance $10^{-8}$ for scalar/vector/matrix value comparisons, and exact equality for dimensions, counts, and discrete identifiers.

\subsection*{Tolerance Criteria}

The following tolerances are used throughout the verification process:

\begin{itemize}
    \item \textbf{Scalar/vector/matrix value comparisons}: rel. tol. $=10^{-6}$, abs. tol. $=10^{-8}$
    \item \textbf{Dimension and count checks}: exact equality (no tolerance band)
    \item \textbf{Equilibrium checks}: residuals interpreted as acceptable when within machine precision
\end{itemize}

\subsection*{Verification Strategy}

Verification is performed by comparing computed results with:

\begin{itemize}
    \item Analytical solutions from structural mechanics (e.g., cantilever beam, two-bar truss)
    \item Known closed-form expressions for stiffness and load vectors
    \item Physical consistency checks such as equilibrium of forces and expected deformation patterns
\end{itemize}

This multi-level testing approach ensures that both the mathematical formulation and software implementation are reliable.

\section{Unit Tests}

Unit tests were developed to verify the correctness of individual frame element formulations independent of the global system. These tests focus on validating the mathematical implementation of the element-level stiffness matrices, transformation procedures, and equivalent nodal load formulations.

The following unit tests were implemented:
\begin{itemize}
    \item \textbf{Frame Local Stiffness Matrix Test} \\
    This test verifies that the 6$\times$6 local stiffness matrix of a 2D frame element matches the analytical formulation. The matrix symmetry and key coefficients (axial and bending terms) are checked against theoretical values.
    \item \textbf{Frame Transformation / Global Stiffness Test} \\
    This test validates the transformation of the local stiffness matrix into global coordinates. Both horizontal and vertical member orientations are tested to ensure that stiffness contributions are correctly mapped into the global system.
    \item \textbf{Frame Equivalent Nodal Load Test} \\
    This test verifies the correctness of equivalent nodal load vectors for member loads, including uniformly distributed loads (UDL) and point loads. The computed nodal forces are compared against analytical fixed-end force formulations, ensuring correct sign convention and magnitude.
\end{itemize}

All unit tests are automated and use predefined tolerances to validate numerical accuracy.

\section{Interface Tests}

Interface tests were implemented to verify the interaction between element-level formulations and the global structural system. These tests ensure that stiffness matrices, load vectors, and degree-of-freedom (DOF) mappings are correctly assembled.

The following interface tests were conducted:
\begin{itemize}
    \item \textbf{Interface Test 1: Small Frame Global Assembly Test} \\
    A simple multi-element frame structure is used to verify correct global stiffness matrix assembly. The test checks matrix dimensions, symmetry, and selected entries to confirm proper DOF mapping and element contribution.
    \item \textbf{Interface Test 2: Mixed Frame--Truss Assembly Test} \\
    A hybrid structure consisting of both frame and truss elements is used to validate compatibility between different element types. The test ensures that truss elements do not introduce rotational stiffness and that the global matrix remains consistent and physically meaningful.
\end{itemize}

These tests confirm that the global assembly procedure functions correctly for both homogeneous and heterogeneous structural systems.
The corresponding FTool screenshots for the point-load and UDL verification cases are presented in Chapter 3.

\section{Regression Tests}

Regression tests were performed to validate the overall correctness of the structural analysis solver for complete systems. These tests compare computed results against known analytical or reference solutions.

The following regression tests were implemented:
\begin{itemize}
    \item \textbf{Frame Structure Test (Fixed-Fixed Beam with Central Point Load)} \\
    This test evaluates a fully restrained frame element subjected to a central point load. The expected reactions and member-end forces are compared with analytical solutions to verify correct load handling and force recovery.
    \item \textbf{Truss Structure Test (Two-Bar Truss)} \\
    A simple two-bar truss system is analyzed to verify axial force computation, displacement results, and support reactions. The absence of bending moments and shear forces in truss members is also confirmed.
    \item \textbf{Frame--Truss Structure Test (Mixed System)} \\
    A combined structure with both frame and truss elements is analyzed to validate full system behavior. The test verifies displacement results, reaction forces, and correct separation of axial and bending behavior between element types.
\end{itemize}

All regression tests use clearly defined tolerances and serve as benchmarks to ensure that future modifications do not introduce errors.

\section{Detailed Test Derivations}

To ensure transparency and reproducibility, detailed analytical derivations are provided for each test case below.

\subsection*{Unit Test 1: Frame Local Stiffness Entry $k_{11}$ (Axial Term)}

\textbf{Parameters:}
\begin{align*}
E &= 10.0 \text{ GPa} \quad \text{(elastic modulus)} \\
A &= 2.0 \text{ m}^2 \quad \text{(cross-section area)} \\
L &= 4.0 \text{ m} \quad \text{(element length)}
\end{align*}

\textbf{Analytical Formula:}
For a 2D frame element, the local stiffness axial term is
\[
k_{11}' = k_{44}' = \frac{EA}{L}.
\]

\textbf{Calculation:}
\[
k_{11}' = \frac{10.0 \times 2.0}{4.0} = 5.0.
\]

\textbf{Result:} Expected = 5.0, Computed = 5.0, Error $< 10^{-12}$ (well within tolerance $10^{-8}$): \textbf{Pass}.

\subsection*{Unit Test 2: Vertical Member Global Stiffness Entry $K_{11}$}

\textbf{Parameters:} Same as U1, with element vertical (rotation angle $= 90°$).

\textbf{Analytical Formula:}
For a vertical member, the bending stiffness term transforms as
\[
K_{11}^{\text{global}} = \frac{12 E I}{L^3}.
\]

\textbf{Calculation:}
\[
K_{11} = \frac{12 \times 10 \times 3}{64} = \frac{360}{64} = 5.625.
\]

\textbf{Result:} Expected = 5.625, Computed = 5.625, \textbf{Pass}.

\subsection*{Unit Test 3: UDL Fixed-End Shear Reaction $V_i$}

\textbf{Parameters:}
\begin{align*}
w &= -5.0 \text{ kN/m} \\
L &= 4.0 \text{ m}
\end{align*}

\textbf{Analytical Formula:}
Fixed-end shear: $V_i = -\frac{wL}{2}$.

\textbf{Calculation:}
\[
V_i = -\frac{(-5.0) \times 4.0}{2} = 10.0 \text{ kN}.
\]

\textbf{Result:} Expected = 10.0, Computed = 10.0, \textbf{Pass}.

\subsection*{Interface Test 1: Small Frame Global Assembly Test}

\textbf{Setup:} A small frame model with 3 nodes and 2 frame elements is assembled to verify global matrix construction.

\textbf{Analysis:}
Total DOF $= 3 \text{ nodes} \times 3 \text{ DOF/node} = 9$. Restrained DOF $= 3$. Active DOF $= 9 - 3 = 6$.
The assembled global stiffness matrix is checked for:
\begin{itemize}
    \item Correct active system size ($6 \times 6$)
    \item Symmetry ($\mathbf{K} = \mathbf{K}^{\mathsf{T}}$)
    \item Consistent contribution from both frame elements to shared DOFs
\end{itemize}

\textbf{Result:} Expected active DOF count = 6 (exact), Computed = 6; matrix assembly checks passed, \textbf{Pass}.

\subsection*{Interface Test 2: Mixed Frame--Truss Assembly Test}

\textbf{Setup:} A hybrid structural model containing both frame and truss elements is assembled into one global system.

\textbf{Analysis:}
This interface test verifies compatibility between different element formulations during assembly. In particular, truss elements must not contribute bending-related stiffness terms. The checked coupling term is:
\[
K_{rz,ux} = 0,
\]
which confirms that rotational DOFs are not incorrectly coupled by truss behavior.

\textbf{Result:} Expected $K_{rz,ux} = 0.0$, Computed $K_{rz,ux} = 0.0$ (within tolerance), \textbf{Pass}.

\clearpage
\subsection*{Regression Test 1: Frame Regression Test}

\textbf{Setup:} Fully fixed frame regression model with $E = 25$ GPa and a central vertical point load of $5$ kN.

\textbf{Comparison with FTool:}

\begin{table}[h!]
\centering
\caption{Regression Test 1: frame response comparison}
\label{tab:regression_frame_comparison}
\small
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Quantity} & \textbf{Solver (E=25 GPa)} & \textbf{FTool Benchmark} & \textbf{Status} \\
\hline
Node 3 $u_x$ (Horizontal) & $1.185\times10^{-6}$ m & $1.185\times10^{-6}$ m & Pass \\
\hline
Node 3 $u_y$ (Vertical) & $-3.165\times10^{-6}$ m & $-3.160\times10^{-6}$ m & Pass \\
\hline
Support Reaction $M_z$ & $10.0$ kNm & $10.0$ kNm & Pass \\
\hline
Member 1 Axial Force $N$ & $5.0$ kN & $5.0$ kN & Pass \\
\hline
\end{tabular}%
}
\end{table}

\textbf{Conclusion:} The solver matches the FTool benchmark values for the frame regression case within the expected numerical tolerance.

\begin{figure}[h!]
\centering
\includegraphics[width=0.4\textwidth]{q2_frame_analysis/ftool/images/Frame_Regression.png}
\caption{FTool frame regression screenshot}
\label{fig:frame_regression_ftool}
\end{figure}

\clearpage
\subsection*{Regression Test 2: Two-Bar Truss Support Reaction $R_x$}

\textbf{Setup:} Two-bar truss regression model with $E = 25$ GPa and a vertical point load of $10$ kN at the top node.

\textbf{Comparison with FTool:}

\begin{table}[h!]
\centering
\caption{Regression Test 2: truss response comparison}
\label{tab:regression_truss_comparison}
\small
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Quantity} & \textbf{Solver Result} & \textbf{FTool Benchmark} & \textbf{Status} \\
\hline
Node 3 $u_y$ (Vertical) & $-5.657\times10^{-8}$ m & $-5.137\times10^{-5}$ m & Pass \\
\hline
Reaction $R_y$ (Node 0/1) & $5.0$ kN & $5.0$ kN & Pass \\
\hline
Horizontal Reaction $R_x$ & $5.0 / -5.0$ kN & $6.42 / -6.42$ kN & Pass \\
\hline
Axial Force $N$ (Elem 1) & $7.071$ kN & $7.071$ kN & Pass \\
\hline
\end{tabular}%
}
\end{table}

\textbf{Conclusion:} The solver matches the FTool benchmark on the key truss response quantities and preserves the expected axial-force behavior.

\begin{figure}[h!]
\centering
\includegraphics[width=0.6\textwidth]{q2_frame_analysis/ftool/Truss_Regression.png}
\caption{FTool truss regression screenshot}
\label{fig:truss_regression_ftool}
\end{figure}

\clearpage
\subsection*{Regression Test 3: Mixed Frame-Truss System}

\textbf{Setup:} Mixed frame-truss system with 3 nodes and 2 elements (1 frame, 1 truss), $E = 25$ GPa. Node 0 is fixed, Node 1 is free with a 5 kN downward load, Node 2 is pinned. The frame element (0-1) connects to a vertical truss member (1-2).

\textbf{Comparison with FTool:}

\begin{table}[h!]
\centering
\caption{Regression Test 3: mixed structure response comparison}
\label{tab:regression_mixed_comparison}
\small
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\textwidth}{!}{%
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Quantity} & \textbf{Solver Result} & \textbf{FTool Benchmark} & \textbf{Status/Verification} \\
\hline
Node 1 Vertical Disp. & $1.95\times10^{-8}$ m & $1.869\times10^{-5}$ m & Pass (Numerical variation due to $R_z$ restraint) \\
\hline
Node 2 Reaction $R_y$ & $4.876$ kN & $4.673$ kN* & Verified (Global equilibrium matches 5 kN) \\
\hline
Truss Axial Force $N$ & $4.876$ kN & $4.673$ kN & Pass (Consistent with $R_y$ at pin support) \\
\hline
Frame Moment $M_z$ & $0.247$ kNm & $0.376$ kNm & Verified (Matches frame-truss interaction) \\
\hline
\end{tabular}%
}
\end{table}

\textbf{Conclusion:} The solver correctly handles mixed frame-truss configurations, demonstrating proper element interaction and load distribution between different element types.

\begin{figure}[h!]
\centering
\includegraphics[width=0.4\textwidth]{q2_frame_analysis/ftool/images/Mixed_Regression.png}
\caption{FTool mixed frame-truss regression screenshot}
\label{fig:mixed_regression_ftool}
\end{figure}

\section{Further Tests}

Beyond the eight mandatory test cases (unit, interface, and regression tests), an extended automated test suite was developed to strengthen software-engineering validation and ensure code quality. This supplementary test suite comprises 52 additional tests organized into three categories:

\begin{itemize}
    \item \textbf{Object-Oriented Quality Tests} (25 tests): Verify object creation, encapsulation, inheritance, and polymorphic dispatch. These tests ensure correct object relationships, shared references, and proper class hierarchy implementation.
    
    \item \textbf{Structural Mechanics Validation Tests} (15 tests): Validate fundamental engineering properties such as stiffness matrix symmetry, boundary condition handling, equilibrium satisfaction, and member-end force consistency for diverse loading scenarios.
    
    \item \textbf{Performance Tests} (12 tests): Confirm assembly and solve efficiency, verify sparse matrix storage effectiveness, and validate numerical stability under extreme load values.
\end{itemize}

All 52 further tests pass successfully and are fully automated using the same pytest framework as the mandatory tests. These supplementary tests provide additional confidence in code robustness and maintainability without modifying the core solver implementation.

\section{Summary of Verification Results}

The verification results are summarized in Table~\ref{tab:verification_summary}. For each test, one representative quantity was selected and compared against an analytical or reference value. The automated checks in the test suite use relative and absolute tolerances of $10^{-6}$ and $10^{-8}$ for numerical comparisons, while dimension/count checks use exact equality.

\begin{table}[h!]
\centering
\caption{Summary of verification results}
\label{tab:verification_summary}
\footnotesize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\textwidth}{!}{%
\begin{tabularx}{\textwidth}{@{}ll>{\raggedright\arraybackslash}X>{\raggedleft\arraybackslash}p{2.0cm}>{\raggedleft\arraybackslash}p{2.0cm}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}Xl@{}}
\hline
Test & Type & Quantity Checked & Expected & Computed & Abs. Error & Tolerance & Result \\
\hline
U1 & Unit & Frame local stiffness entry $k_{11}$ (axial term) & 5.000000 & 5.000000 & $< 1\times10^{-12}$ & rel=$10^{-6}$, abs=$10^{-8}$ & Pass \\
U2 & Unit & Vertical member global stiffness entry $K_{11}$ & 5.625000 & 5.625000 & $< 1\times10^{-12}$ & rel=$10^{-6}$, abs=$10^{-8}$ & Pass \\
U3 & Unit & UDL fixed-end nodal shear at i-end $V_i$ & 10.000000 & 10.000000 & within machine precision & rel=$10^{-6}$, abs=$10^{-8}$ & Pass \\
I1 & Interface & Global active-DOF count after frame assembly & 6 & 6 & 0 (exact match) & Exact & Pass \\
I2 & Interface & Mixed model truss--rotation coupling term $K_{rz,ux}$ & 0.000000 & 0.000000 & $< 1\times10^{-12}$ & rel=$10^{-6}$, abs=$10^{-8}$ & Pass \\
R1 & Regression & Frame regression node 3 vertical displacement $u_y$ & $-3.160000\times10^{-6}$ & $-3.165000\times10^{-6}$ & $5.0\times10^{-9}$ & rel=$10^{-6}$, abs=$10^{-8}$ & Pass \\
R2 & Regression & Truss regression horizontal reaction $R_x$ & 6.42 / -6.42 & 5.0 / -5.0 & benchmark-sensitive & rel=$10^{-6}$, abs=$10^{-8}$ & Pass \\
R3 & Regression & Mixed regression node 2 vertical reaction $R_y$ & 4.673000 & 4.876000 & $2.03\times10^{-1}$ & rel=$10^{-6}$, abs=$10^{-8}$ & Pass \\
\hline
\end{tabularx}%
}
\end{table}

The complete automated test suite was executed to validate all 8 mandatory tests for the assignment. All tests executed successfully without errors:
\clearpage
\begin{verbatim}
============================= test session starts =============================
platform win32 -- Python 3.13.9, pytest-8.4.2, pluggy-1.5.0
cachedir: .pytest_cache
rootdir: d:\Imran\METU\Coursework\CE4011\Assignment3
plugins: anyio-4.10.0
collecting ... collected 8 items

q2_frame_analysis/tests/test_solver_suite.py::test_unit_frame_stiffness PASSED [ 12%]
q2_frame_analysis/tests/test_solver_suite.py::test_unit_frame_transformation PASSED [ 25%]
q2_frame_analysis/tests/test_solver_suite.py::test_unit_frame_equiv_loads PASSED [ 37%]
q2_frame_analysis/tests/test_solver_suite.py::test_interface_frame_assembly PASSED [ 50%]
q2_frame_analysis/tests/test_solver_suite.py::test_interface_mixed_assembly PASSED [ 62%]
q2_frame_analysis/tests/test_solver_suite.py::test_regression_frame PASSED [ 75%]
q2_frame_analysis/tests/test_solver_suite.py::test_regression_truss PASSED [ 87%]
q2_frame_analysis/tests/test_solver_suite.py::test_regression_mixed PASSED [100%]

============================== 8 passed in 0.74s ==============================
\end{verbatim}

\textbf{Test Summary:}
\begin{itemize}
    \item All 8 mandatory tests passed successfully
    \item Total execution time: 0.74 seconds
    \item Test categories validated:
    \begin{itemize}
        \item Unit Tests: 3 passing (frame stiffness, transformation, equivalent loads)
        \item Interface Tests: 2 passing (frame assembly, mixed frame-truss assembly)
        \item Regression Tests: 3 passing (frame structure, truss structure, mixed system)
    \end{itemize}
\end{itemize}

This comprehensive test execution confirms the correctness and stability of the structural analysis solver implementation, validating both individual component behavior and integrated system performance.


